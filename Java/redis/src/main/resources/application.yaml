spring:
  application:
    name: redis-service

  datasource:
    username: root
    password: "Dev1234!"
    url: jdbc:mysql://192.168.3.19:3306/volunteer?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 192.168.3.19
      port: 6379
      password: "Dev1234!"
      database: 0
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 1s

# mybatis
mybatis-plus:
  mapper-locations:
    - "classpath*:/mapper/*.xml"
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: info
    # com.baomidou.mybatisplus: debug
